<Window x:Class="Autocad_Primavera_P6_Plugin.UserInterface.UI_InsertBlocksWindowView.InsertBlocksWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Autocad_Primavera_P6_Plugin.UserInterface.UI_InsertBlocksWindowView"
        d:DataContext="{d:DesignInstance Type=local:InsertBlocksWindowViewModel}"
        mc:Ignorable="d"
        Title="Insert Blocks" Height="450" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Available Blocks" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding AvailableBlocks}"
                 SelectedItem="{Binding SelectedBlock, Mode=TwoWay}"
                 Margin="0,0,0,10"
                 MinHeight="200"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Insert Block" Width="100" Margin="0,0,10,0"
                    Command="{Binding InsertBlockCommand}"/>
        </StackPanel>
    </Grid>
</Window>
