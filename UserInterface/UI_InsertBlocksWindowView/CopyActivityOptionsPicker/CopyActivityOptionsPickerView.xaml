<Window x:Class="Autocad_Primavera_P6_Plugin.UserInterface.UI_InsertBlocksWindowView.CopyActivityOptionsPicker.CopyActivityOptionsPickerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copy Activity Options"
        Width="640" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Autocad_Primavera_P6_Plugin.UserInterface.UI_InsertBlocksWindowView.CopyActivityOptionsPicker" 
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:CopyActivityOptionsPickerViewModel}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Instruction banner -->
        <Border Grid.Row="0" BorderBrush="DarkGray" BorderThickness="1" Padding="8" Margin="0,0,0,12">
            <TextBlock Text="Specify the Activity information to be copied" FontWeight="SemiBold"/>
        </Border>

        <!-- Options + action buttons -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left column -->
            <StackPanel Grid.Column="0" Margin="20,0,10,0">
                <CheckBox Content="Resource &amp; Role Assignments" IsChecked="{Binding Model.ResAndRoleAssignments}" Margin="0,5"/>
                <Grid Height="18" Margin="0,5" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Path Grid.Column="0" Stroke="Gray" StrokeThickness="1" Data="M10,0 L10,9 L20,9"/>
                    <CheckBox 
                        Grid.Column="1" 
                        Content="Assignment Codes" 
                        IsEnabled="{Binding Model.ResAndRoleAssignments}"
                        IsChecked="{Binding Model.ResAndRoleAssignments_AssignmentCodes}"/>
                </Grid>
                <CheckBox Content="Relationships" IsChecked="{Binding Model.Relationships}" Margin="0,5"/>
                <Grid Height="18" Margin="0,5" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Path Grid.Column="0" Stroke="Gray" StrokeThickness="1" Data="M10,0 L10,9 L20,9"/>
                    <CheckBox 
                        Grid.Column="1" 
                        Content="Only between copied activities" 
                        IsEnabled="{Binding Model.Relationships}"
                        IsChecked="{Binding Model.Relationships_OnlyBetweenCopiedActivities}"/>
                </Grid>
                <CheckBox Content="Expenses" IsChecked="{Binding Model.Expenses}" Margin="0,5"/>
                <CheckBox Content="Activity Codes" IsChecked="{Binding Model.ActivityCodes}" Margin="0,5"/>
            </StackPanel>

            <!-- Right column -->
            <StackPanel Grid.Column="1" Margin="10,0">
                <CheckBox Content="Notebook" IsChecked="{Binding Model.Notebook}" Margin="0,5"/>
                <CheckBox Content="Steps" IsChecked="{Binding Model.Steps}" Margin="0,5"/>
                <CheckBox Content="Financial Period Data" IsChecked="{Binding Model.FinancialPeriodData}" Margin="0,5"/>
                <CheckBox Content="WPs &amp; Docs" IsChecked="{Binding Model.WPsAndDocs}" Margin="0,5"/>
                <CheckBox Content="Risks" IsChecked="{Binding Model.Risks}" Margin="0,5"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Column="2" Margin="16,0,0,0">
                <Button Content="OK" Command="{Binding OkButtonCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Width="85" Margin="0,0,0,8"/>
                <Button Content="Cancel" Command="{Binding CancelButtonCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Width="85" Margin="0,0,0,8"/>
            </StackPanel>
            
        </Grid>
    </Grid>
    
</Window>
