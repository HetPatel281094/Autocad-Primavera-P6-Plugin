<Window x:Class="Autocad_Primavera_P6_Plugin.UserInterface.UI_PluginStatusWindowView.PluginStatusWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Autocad_Primavera_P6_Plugin.UserInterface.UI_PluginStatusWindowView"
        d:DataContext="{d:DesignInstance Type=local:PluginStatusWindowViewModel}"
        mc:Ignorable="d"
        Title="Plugin Status" Height="450" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Plugin Status" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding PluginStatuses}"
                 Margin="0,0,0,10"
                 MinHeight="200"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Refresh Status" Width="110" Margin="0,0,10,0"
                    Command="{Binding RefreshStatusCommand}"/>
        </StackPanel>
    </Grid>
</Window>
